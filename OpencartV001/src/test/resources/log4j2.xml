<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    
    <!-- Appenders: where logs will be written -->
    <Appenders>
        
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="[%d{yyyy-MM-dd HH:mm:ss}] [%p] %c{1} - %m%n"/>
        </Console>
        
        <!-- Rolling File Appender -->
        <RollingFile name="FileLogger"
                     fileName="logs/automation.log"
                     filePattern="logs/automation-%d{yyyy-MM-dd-HH-mm}.log.gz">
            <PatternLayout>
                <pattern>[%d{yyyy-MM-dd HH:mm:ss}] [%p] %c{1} - %m%n</pattern>
            </PatternLayout>
            <Policies>
                <!-- Rotate when file size exceeds 10 MB -->
                <SizeBasedTriggeringPolicy size="10 MB"/>
                <!-- Rotate daily -->
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
        </RollingFile>
        
    </Appenders>
    
    <!-- Loggers: define logging levels -->
    <Loggers>
        
        <!-- Selenium / Test logs -->
        <Logger name="com.opencartv001" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
        </Logger>
        
        <!-- Root logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
        </Root>
        
    </Loggers>
</Configuration>